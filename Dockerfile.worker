FROM node:20-alpine
WORKDIR /app

RUN apk add --no-cache curl

COPY package.json package-lock.json* ./
RUN npm ci

COPY . .
RUN npm run db:generate

ENV WORKER_TARGET=server/workers/metrics.ts

CMD ["sh", "-c", "npx tsx ${WORKER_TARGET}"]
